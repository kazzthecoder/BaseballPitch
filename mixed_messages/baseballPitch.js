const pitch() => {
    const results = ['ball', 'strike', 'foul ball', 'single', 'double', 'triple', 'HOME RUN!!', 'ground out', 'fly out', 'DOUBLE PLAY!'];
    randNumber = Math.floor(Math.random()*11);
    console.log(result[randNum])
}
pitch()